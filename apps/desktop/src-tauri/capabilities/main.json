{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window-events",
  "description": "Allow the main local webview to receive native host state updates.",
  "windows": ["main"],
  "permissions": [
    "core:event:allow-listen",
    "core:event:allow-unlisten"
  ]
}
